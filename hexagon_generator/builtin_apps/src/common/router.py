from fastapi import APIRouter

api_router = APIRouter()

# Built-in module routers are registered here when installed via generate_builtin.
# CRUD module routers are registered here when generated via generate_crud.
